# توصيات واقتراحات — منصة الفردوس

هذا الملف يجمع أهم التوصيات والاقتراحات لتحسين المنصة وتقويتها، مرتبة حسب الأولوية
(الأمان ثم الاستقرار ثم تجربة المستخدم ثم التوسّع). بعض هذه البنود موثّقة أيضاً ضمن
قسم «المشاكل المعروفة» في `README.md`، وتُترك هنا كخارطة طريق للعمل القادم.

---

## 1) الأمان والتحصين (أولوية قصوى)

1. **حماية `/api/roles`** — يُرجع حالياً قائمة الأدوار الكاملة (بما فيها مصفوفات
   الصلاحيات) لأي زائر دون تسجيل دخول. يُنصح بإضافة فحص الجلسة `getSession()`.
2. **حماية `POST /api/scan-logs`** — يقبل البيانات دون تحقق من الجلسة. يجب إجبار
   جلسة صالحة قبل القبول.
3. **حماية `GET /api/download/db`** — مخصّص للمدير لكنه لا يتحقق من الجلسة، ويعتمد
   على برنامج `pg_dump` على الخادم. يُنصح بمنع الوصول الخارجي أو إضافة فحص صلاحيات
   إداري، أو الاستغناء عنه والاكتفاء بالنسخة الاحتياطية JSON أو لوحة تحكم قاعدة البيانات.
4. **تقييد القراءة حسب الدور** — نقاط `GET /api/dashboard/stats` و`/api/students`
   و`/api/finance/fees` متاحة لأي جلسة صالحة وليس فقط للمدير. يُنصح بتقييدها بالصلاحيات
   بحيث يرى ولي الأمر بياناته فقط.
5. **مفتاح الجلسة** — `lib/auth.ts` يستخدم قيمة احتياطية ثابتة عند غياب `SESSION_SECRET`.
   يجب وضع `SESSION_SECRET`/`AUTH_SECRET` قوي في الإنتاج.

---

## 2) استقرار وتنقية البيانات

6. **تنظيف مفاتيح الإعدادات القديمة** — توجد صفوف متروكة في قاعدة البيانات
   (`barcode_idle_enabled`, `barcode_idle_seconds`) من ميزة سابقة أُلغيَت. تُعتمد الآن
   مفاتيح `idle_display_*` و`adhan_*` الجديدة؛ يُنصح بحذف القديمة أو إهمالها نهائياً.
7. **التحقق من مصادرة وقت الأذان** — نافذة تشغيل الأذان (30 ثانية) تفترض أن جهاز
   الباركود مفتوح على شاشة الخمول. يُنصح باختبار سلوك «تشغيل الأذان» على جهاز فعلي
   وتأكيد أن المتصفح يسمح بالصوت (autoplay) بعد تفاعل سابق للمستخدم.
8. **إضافة اختبارات آلية** — لا توجد حالياً اختبارات وحدة/تكامل. يُنصح بإضافة تغطية
   على منطق مواقيت الصلاة (التحويلات، نافذة الأذان→الإقامة، انتقال التوهج) وحسابات
   النسخ الاحتياطي.

---

## 3) تجربة المستخدم والواجهة

9. **صوت الأذان** — ملف الأذان الحالي (من praytimes.org، سريع وأنيق) مُضمّن محلياً
   ليعمل دون إنترنت. يُنصح بتوفير قائمة «أصوات أذان» جاهزة في الإعدادات بدل إدخال رابط
   يدوي، مع معاينة للصوت قبل الحفظ.
10. **إدارة الوقت** — فكرة مفيدة: زر «تسجيل/إيقاف صوت الأذان يدوياً» و«إظهار شاشة
    الصلاة الآن» من داخل شاشة الباركود للمسؤول حتى في غير أوقات الخمول.
11. **تحسين دقة التوهج** — عند بلوغ وقت الإقامة ينتقل التوهج فوراً للصلاة التالية
    بينما يستمر تنبيه الوميض للصلاة المنتهية؛ يمكن جعل التوهج ملازماً للتنبيه حتى
    نهاية الخمس ثوانٍ ثم الانتقال، بحسب تفضيل الإدارة.

---

## 4) الأداء والتوسّع

12. **تقليل إعادة الحساب** — تُحسب أوقات الصلاة كل ثانية مع تحديث الساعة؛ يُنصح بحساب
    مواقيت اليوم مرة واحدة ودمج تحديث الساعة فقط، لتحسين الأداء على الأجهزة المنخفضة.
13. **التخزين المحلي** — الخاصية حالياً `local-first` للمسح دون اتصال؛ يُنصح بتوسيعها
    لتشمل طلبات التسجيل والرسائل القادمة.
14. **الإشعارات** — يُنصح بتفعيل VAPID على نطاق إنتاج فعلي واختبار Web Push على
    أجهزة iOS/Android.

---

## 5) النشر

15. **المتغيرات الحساسة** — حزمة `/ferdous-platform.zip` لا تحتوي أسراراً؛ تأكد دائماً
    من `cp .env.example .env` ووضع `DATABASE_URL` و`AUTH_SECRET` الفعليين قبل النشر.
16. **النسخ الاحتياطي** — أخذ نسخة JSON أسبوعياً على الأقل، والاعتماد على لوحة تحكم
    قاعدة البيانات (Neon) للتفريغ الكامل.

---

## خلاصة

أولوية العمل المقترحة:
1. **معالجة بنود الأمان 1–5** قبل أي توسّع.
2. **تنظيف الإعدادات القديمة + اختبارات** على منطق المواقيت.
3. **تحسين تجربة شاشة الخمول** (أصوات جاهزة، أزرار يدوية، سلوك التوهج).
4. **تحسينات الأداء والنشر** المستمرة.

---

Based on vibe-web-template by HappySeeds.
Create with HappySeeds: https://happyseeds.ai
